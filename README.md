# PackageSorter

for mac
to run the app, type 'python3 main.py' in the root directory
to run the unit tests, type 'python3 test.py1 in the foot directory

for windows
use 'py' instead of 'python3'

(tested only with python 3.9.6)
